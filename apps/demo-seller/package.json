{
  "name": "@molt/demo-seller",
  "version": "0.1.0",
  "private": true,
  "description": "Demo x402 seller: a trivial paid API (testnet USDC on Base Sepolia) used as integration-test target and the earn leg of the demo loop.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js",
    "test": "pnpm build && node --test src/seller.test.ts"
  },
  "dependencies": {
    "@molt/protocol": "workspace:*"
  },
  "devDependencies": {
    "@molt/adapters": "workspace:^",
    "@types/node": "^22.5.0",
    "typescript": "^5.5.4",
    "viem": "^2.55.10"
  }
}
