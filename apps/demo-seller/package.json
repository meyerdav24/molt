{
  "name": "@molt/demo-seller",
  "version": "0.1.0",
  "private": true,
  "description": "Demo x402 seller: a trivial paid API (testnet USDC on Base Sepolia) used as integration-test target and the earn leg of the demo loop.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js",
    "test": "echo 'no tests yet (OT-113)'"
  },
  "dependencies": {
    "@molt/protocol": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "typescript": "^5.5.4"
  }
}
